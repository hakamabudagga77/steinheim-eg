export default function AdminContentPage() {
  return (
    <div>
      <p className="text-[11px] uppercase tracking-[0.3em] text-black/40">Content</p>
      <h1 className="mt-2 font-heading text-[32px] tracking-[-0.02em]">Coming soon.</h1>
      <p className="mt-4 max-w-lg text-[14px] leading-[1.7] text-black/50">
        Editing site copy without touching code is a real architecture decision — page content
        currently lives in the app&apos;s source files, not a database. Worth its own conversation
        before building.
      </p>
    </div>
  );
}

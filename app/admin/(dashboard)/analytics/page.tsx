export default function AdminAnalyticsPage() {
  return (
    <div>
      <p className="text-[11px] uppercase tracking-[0.3em] text-black/40">Analytics</p>
      <h1 className="mt-2 font-heading text-[32px] tracking-[-0.02em]">Coming soon.</h1>
      <p className="mt-4 max-w-lg text-[14px] leading-[1.7] text-black/50">
        GA4 tracking is now live site-wide. A traffic dashboard here needs a GA4 property ID and a
        Google Cloud service account with Analytics Data API access — ask for exactly what to
        provide when you&apos;re ready to build this out.
      </p>
    </div>
  );
}
